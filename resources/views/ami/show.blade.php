@extends('layouts.app')
@section('content')
<div class="d-flex justify-content-between py-3 mb-4"><h4 class="fw-bold">Detail AMI</h4><a href="{{ route('ami.index') }}" class="btn btn-secondary">Kembali</a></div>
<div class="card mb-4"><div class="card-body"><x-detail-row label="Tahun Akademik">{{ $ami->tahunAkademik?->nama }}</x-detail-row><x-detail-row label="Tanggal Pelaksanaan">{{ $ami->tanggal_pelaksanaan?->format('d-m-Y') }}</x-detail-row><x-detail-row label="Temuan"><span style="white-space:pre-line">{{ $ami->temuan }}</span></x-detail-row><x-detail-row label="Rekomendasi"><span style="white-space:pre-line">{{ $ami->rekomendasi }}</span></x-detail-row><x-detail-row label="Tindak Lanjut"><span style="white-space:pre-line">{{ $ami->tindak_lanjut ?: '-' }}</span></x-detail-row><x-detail-row label="Target Selesai">{{ $ami->target_selesai?->format('d-m-Y') ?? '-' }}</x-detail-row><x-detail-row label="Status"><span class="badge bg-label-primary">{{ ucfirst($ami->status) }}</span></x-detail-row><x-detail-row label="Penginput">{{ $ami->penginput?->name ?? '-' }}</x-detail-row></div></div>
<div class="card"><h5 class="card-header">Bukti AMI</h5><div class="card-body">@forelse($ami->dokumenAmis as $dokumen)<div class="border rounded p-3 mb-2"><strong>{{ $dokumen->nama_dokumen }}</strong>@if($dokumen->file_path)<a href="{{ asset('storage/'.$dokumen->file_path) }}" target="_blank" class="btn btn-sm btn-outline-primary ms-2">File</a>@endif @if($dokumen->link_url)<a href="{{ $dokumen->link_url }}" target="_blank" rel="noopener noreferrer" class="btn btn-sm btn-outline-info ms-1">Google Drive</a>@endif<br><small>Diunggah oleh {{ $dokumen->pengunggah?->name ?? '-' }}</small></div>@empty<span class="text-muted">Belum ada bukti.</span>@endforelse</div></div>
@endsection
